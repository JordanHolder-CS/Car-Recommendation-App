import { MaterialIcons } from "@expo/vector-icons";

// use https://fonts.google.com/icons for icons

const Icons = ({ icon }) => {
  return <MaterialIcons name={icon} size={12} color="#5d5d5d" />;
};

export default Icons;
